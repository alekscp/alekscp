# Local deployment values. Copy to .env when running Wrangler locally.
# GitHub Actions should use repository secrets with the same names.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_WORKER_NAME=alekscp-com
CLOUDFLARE_ZONE_NAME=alekscp.com
